# Backend origin, no trailing slash. Leave unset for local dev — Vite's dev
# server proxies /api to http://localhost:3001 (see vite.config.js).
# In production, point this at your deployed backend, e.g.:
# VITE_API_URL=https://autonote-backend.onrender.com
VITE_API_URL=
